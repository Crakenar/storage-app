{
  "name": "uploadproject",
  "version": "1.0.0",
  "description": "a project that simply expose endpoints to retrieve and upload files to a docker",
  "main": "app.js",
  "scripts": {
    "start": "node app.ts",
    "dev": "nodemon --legacy-watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "db-migrate": "^0.11.13",
    "db-migrate-mysql": "^2.2.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "mysql": "^2.18.1",
    "nodemon": "^3.0.1"
  }
}
